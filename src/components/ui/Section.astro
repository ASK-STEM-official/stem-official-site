---
import { cn } from '../../lib';

interface Props {
  class?: string;
}

const { class: className } = Astro.props;
---

<section class={cn("overflow-y-visible", className)}>
  <div class="max-w-7xl mx-auto px-4 py-24">
    <slot />
  </div>
</section>
